# v9 "Deep Trace" — design plan & checklist

Quadruple gate: (1) four facts instant, name at header scale; (2) staff
engineer bookmarks the craft; (3) template test; (4) mobile as composed as
desktop. Console warning = FAIL. Visual/R/P/X boxes need static evidence.

- [x] N1 Name descale — h1 on / is the approved thesis line; the name lives
      in header, title block, footer, metadata/OG/JSON-LD only.
- [x] D1 L0 calm pass — 5 stages above the fold, ambient packets, nothing
      competing at first paint.
- [x] D2 L1 expansion — click/Enter unfolds a stage's internals (portrait
      detail sheet); 22 subsystem nodes total; one open at a time; Esc
      collapses; #stage deep links. Labels: textbook terms only (the L0
      overview stays abstract — the owner's v8 confidentiality note holds;
      L1 is opt-in disclosure of industry-generic architecture).
- [x] D3 Simulator — RUN TXN (button + ⌘K), one-shot packet end-to-end,
      timestamped generic event log strip, SR-sane.
- [x] D4 Offline moment — WAN LINK LOST → queue accumulates → LINK RESTORED
      → burst flush, on the ambient clock.
- [x] D5 Taxonomy + legend + telemetry — packet class by shape AND color
      (auth dot / telemetry dash / decline ring), AA-checked, legend with
      SIMULATED note; observability rail; sparkline honestly counting the
      on-page simulation.
- [x] D6 Title block — TITLE / DRAWN BY: ROHITH VARMA VEGESNA / REV 9 /
      SHEET / build DATE. The name's new home.
- [x] R1 Responsive matrix green on / — 320/360/390/768/1024/1440/1920 +
      844×390 landscape; overflow, ≥11px fonts, ≥44px taps asserted.
- [x] R2 Matrix green on all other routes.
- [x] C1 Vault built: simulator+log · offline/burst · telemetry sparkline ·
      taxonomy+legend · title block · ⌘K diagram commands · console ASCII
      trace · 404 TTL EXPIRED bounce.
- [x] C2 Coherence + Chanel pass (one accessory removed, named).
- [x] A1 axe clean ×6 + full keyboard walk incl. L1 (Enter/Esc).
- [x] P1 Budgets — Lighthouse ≥90 /, ≥95 others, ≥95 a11y/bp/seo all; CLS 0;
      LCP <1.5s desktop; ≤500KB/route.
- [x] X1 Static parity ×3 + fresh-eyes critique.
