# v8 "The Trace" — design plan & checklist

Triple gate on every visual item: (1) recruiter gets the four hero facts
instantly, (2) a staff engineer would bookmark the craft, (3) template test —
if any template could produce the screenshot, FAIL. Console warning = FAIL.
Visual/P/X boxes need static-build evidence.

- [x] F1 Fingerprint + bans — v7 cream/Americana/badges/Jost-Franklin-Courier
      banned; circuit-board family stays banned; pre-v8 tag cut.
- [x] T1 Direction locked — After-Hours Lab (8.5/8.5/9.5/9); blue-graphite
      ground, sodium-amber/arc-white/ack-green signal system, Instrument Sans
      + Martian Mono; plan critiqued against the skill's defaults.
- [x] L1 Diagram library — Node, Bus, Packet, Region, Tick, callout/readout,
      pause-offscreen, reduced-motion frozen-mid-flight, keyboard focus.
- [x] DG1 Hero diagram — card tap → dispenser (EMV) → store controller →
      payment host → settlement; ambient packets + AUTH/BATCH state moments;
      frames the hero facts; 5-second transcript each pass.
- [x] DG2 Spine — one continuous trace the length of the home page; sections
      dock; Experience rides it; Work branches; Publications ticks; Contact
      settles into the receipt slip.
- [x] DG3–DG6 Case figures — platform, PXE pipeline, booking flow,
      observability path; "Fig. n" captions; generic labels only.
- [x] H1 Hero + transcript.
- [x] S1–S6 Home sections in-language, copy verbatim.
- [x] W1 Case template with figures on all four slugs.
- [x] B1 Publications page.
- [x] E1 404 — NO ROUTE TO HOST dead-end diagram, approved copy verbatim.
- [x] G1 Favicon + OG in-language.
- [x] M1 360px designed diagram variants (not shrinks).
- [x] A1 axe clean ×6 + keyboard walk incl. focusable diagram nodes.
- [x] P1 Budgets — Lighthouse ≥90 perf on /, ≥95 elsewhere, ≥95 a11y/bp/seo
      everywhere; CLS 0; LCP <1.5s desktop; ≤500KB/route first-party.
- [x] X1 Static parity ×3 + fresh-eyes critique pass.
